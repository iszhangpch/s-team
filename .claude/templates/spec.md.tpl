# 规格：<功能名称>

> **Spec 的边界**：可以包含 high-level 的技术约束（"需要支持离线访问"、"必须兼容现有的 OAuth 登录"），但不包含实现细节。判断标准：这句话是在约束 Planner 的选择空间，还是在替 Planner 做决定？前者属于 spec，后者不属于。

## 目标
<一段话：我们在解决什么问题、为谁解决、为什么现在解决>

## 用户
<谁会使用这个功能，在什么场景下使用>

## 范围

### 范围内
- <面向用户的行为或业务规则>

### 范围外
- <明确排除的行为>

## 领域知识
<Planner 需要理解的业务规则、术语和领域约束>
- <规则或约束>

## 需求

### 功能性需求
<系统必须做什么，用用户/产品术语描述>
- <需求>

### 非功能性需求
<性能、可靠性、兼容性、安全等约束——描述"要达到什么"，不描述"怎么实现">
- <性能例：在标准网络下，登录必须在 2 秒内完成>
- <兼容性例：必须复用现有的 SSO 登录机制，不引入新的认证体系>
- <约束例：数据必须在用户设备本地加密存储>

## 验收标准
<从用户角度判断功能正常工作的标准>
- <标准>

## 选定方案
<选择了哪种方案，以及原因。可以包含 high-level 的技术方向（如"基于现有消息队列扩展"），但不包含具体实现（不写用哪个库、怎么设计接口、文件怎么组织）>

## 开放问题
<交接给 Planner 之前必须为空>

---

## Spec 自检清单
<写完 spec 后逐项检查，通过后删除本节>

- [ ] **完整性**：无 TBD、TODO、占位符、未完成章节
- [ ] **一致性**：各章节之间无矛盾，需求与验收标准对齐
- [ ] **清晰度**：每条需求只有一种合理解读，无歧义
- [ ] **范围**：聚焦于单一功能，未混入多个独立子系统
- [ ] **YAGNI**：无未被请求的功能，无过度设计
- [ ] **开放问题**：本节为空
