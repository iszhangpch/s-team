# 任务计划：<功能名称>

> **供 Generator 使用：** 逐任务执行，每个任务完成后提交。步骤使用复选框（`- [ ]`）跟踪进度。

**目标：** <一句话描述本次实现的内容>

**架构：** <2-3 句话描述技术方案>

**技术栈：** <关键技术/库>

---

## 文件结构
<列出所有将创建或修改的文件及其职责，在任务分解前锁定边界>

- `创建 exact/path/to/file`: <单一职责>
- `修改 exact/path/to/existing`: <修改原因>
- `测试 tests/exact/path/to/test`: <测试范围>

## 技术方案
<架构决策及原因——技术栈选择、使用的模式、关键权衡>

## 模块设计
<每个新建或重要修改的模块/组件负责什么>
- `模块/路径`: <单一职责>

## 代码库上下文
<该功能基于的相关现有模式、规范和代码>

---

## 任务

### 任务 1：<名称>

**文件：**
- 创建/修改：`精确/路径/到/文件`
- 测试：`精确/路径/到/测试`

**描述：** <要实现什么——具体到 Generator 无需任何澄清>

**接口：**
```
<函数签名、类型定义或 API 契约——精确到参数名和返回类型，不写函数体>
```

**测试描述：** <测试什么行为，覆盖哪些场景和边界条件——Generator 据此编写具体测试代码>

- [ ] **步骤 1：编写失败的测试**
- [ ] **步骤 2：运行测试——确认它失败**

运行：`<精确命令>`
预期：FAIL

- [ ] **步骤 3：编写最小实现使测试通过**
- [ ] **步骤 4：运行测试——确认它通过**

运行：`<精确命令>`
预期：PASS

- [ ] **步骤 5：提交**

```bash
git commit -m "feat: <任务名称>"
```

**验收标准：**
- <标准——可通过运行代码或测试来验证>

---

### 任务 2：<名称>
...

## 依赖关系
<任务顺序——哪些任务阻塞哪些任务>

## 测试计划
<如何运行完整测试套件以及通过时的输出样式>

---

## Task Plan 自检清单
<写完后逐项检查，通过后删除本节>

- [ ] **Spec 覆盖**：每条 spec 需求都能对应到至少一个任务
- [ ] **无占位符**：无 TBD、TODO、"类似任务 N"、"处理边界情况"等模糊描述
- [ ] **接口完整**：每个任务的接口签名精确（参数名、类型、返回值），测试描述覆盖主要场景和边界
- [ ] **类型一致**：跨任务的函数名、类型名、方法签名完全一致
- [ ] **精确路径**：所有文件路径精确，无通配符或模糊描述
- [ ] **精确命令**：所有运行命令包含预期输出
